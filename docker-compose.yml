version: '2'
services:
  proxy_pool:
    build: .
    container_name: proxy_pool
    ports:
      - "5010:5010"
    links:
      - redis
    environment:
      DB_CONN: "redis://@proxy_redis:6379/5"
  proxy_redis:
    image: "redis"
    container_name: redis